import styled from 'styled-components'

import backbg from 'images/profile/backbg.png'

const BackButton = styled.button `
  width: .1rem;
  height: .175rem;
  background: url(${backbg}) center / 100%;
  position: absolute;
  top: .295rem;
  left: .15rem;
`

export default BackButton